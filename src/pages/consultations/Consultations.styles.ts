import styled from 'styled-components';
import { textDarkPurple } from '../../components/core/colours';

const ConsultationsWrapper = styled.div`
  p {
    color: ${textDarkPurple};
  }
`;

export default ConsultationsWrapper;
